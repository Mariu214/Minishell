#include "../../include/minishell.h"

int    new_lexer(t_data *data)
{
    int i;
    data->liste = NULL;

    i = 0;
    while (data->str[i])
    {
        if (data->str())
        {
            data->list->
        }
    }
}

int test_lexer(t_data *data)
{
    int result;

    result = new_lexer(data);
}